# Hoisted layout: flat node_modules without .pnpm's long versioned paths.
# Required so the Next.js standalone server embedded in the Tauri installer
# stays under the Windows 260-char MAX_PATH limit during NSIS install.
node-linker=hoisted
